import React from 'react';

export const TopBar: React.FC = () => {
  return (
    <div className="bg-black border-b border-gray-700 py-4 px-6">
      {/* Empty header - can be used for future features if needed */}
    </div>
  );
};